# 📋 توثيق مشروع "مقهى الشركة" ومتطلبات الباك إند (Backend)

## 1. نبذة عن المشروع
المشروع عبارة عن تطبيق ويب داخلي لشركة يهدف لتنظيم عملية طلب المشروبات من البوفيه/المقهى الخاص بالشركة.
**الحالة الحالية:** يعمل بالكامل على الواجهة الأمامية (Frontend) ويستخدم الـ LocalStorage في المتصفح لحفظ البيانات مؤقتاً.

---

## 2. المميزات الحالية (Frontend Features) - *تم التحديث*
### 👤 واجهة الموظف (User)
*   **تسجيل الدخول الموحد:** عبر إدخال اسم المستخدم وكلمة المرور الخاصة به (التي ينشئها الأدمن).
*   **عرض القائمة:** تصفح المشروبات المتاحة ومعرفة حالة توفرها (متوفر/نفذت الكمية).
*   **سلة الطلبات:** إضافة وعرض عدة أصناف في سلة واحدة.
*   **إرسال الطلب:** يتم تعبئة اسم الموظف تلقائياً بناءً على الحساب، مع إمكانية إضافة ملاحظات.
*   **متابعة الطلبات وتقييمها:**
    *   عرض سجل بطلبات الموظف السابقة وحالتها.
    *   ظهور اسم المسؤول (الأدمن) الذي عالج الطلب.
    *   **نظام التقييم:** إمكانية تقييم الخدمة (من 1 إلى 5 نجوم) للطلبات المكتملة.

### 🔒 واجهة المسؤول (Admin)
*   **تسجيل الدخول:** عبر حساب بصلاحية "أدمن" أو "مدير عام".
*   **الصلاحيات الهرمية (Hierarchical Roles):**
    *   **مدير عام (Super Admin):** له كامل الصلاحيات بما فيها إنشاء وتعديل وحذف الحسابات الأخرى (أدمن/عميل).
    *   **أدمن (Admin):** له صلاحيات إدارة الطلبات والمخزون وإنشاء حسابات عملاء فقط.
*   **إدارة المستخدمين:**
    *   إضافة مستخدمين جدد (اسم مستخدم + كلمة مرور + صلاحية).
    *   عرض قائمة المستخدمين وحذفهم.
*   **إدارة الطلبات:**
    *   عرض الطلبات الجديدة "قيد الانتظار".
    *   قبول/رفض الطلب (يخصم الكمية تلقائياً).
*   **إدارة المخزون:** عرض وتعديل المنتجات والكميات.
*   **التقارير:**
    *   سجل بالطلبات المنفذة.
    *   **تقارير الأداء:** عرض متوسط تقييمات كل أدمن بناءً على تقييمات الموظفين.

---

## 3. المطلوب من الباك إند (Backend Requirements)
لتحويل هذا المشروع من مجرد واجهة (Frontend) إلى تطبيق حقيقي يعمل على سيرفر وقاعدة بيانات، نحتاج لتطوير الـ API وقاعدة البيانات التالية:

### أ) قاعدة البيانات المقترحة (Database Schema)

**1. جدول المستخدمين (Users) - *جديد***
| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | Integer/UUID | المعرف الفريد |
| `username` | String | اسم الدخول (فريد) |
| `password` | Hash | كلمة المرور المشفرة |
| `role` | Enum | الصلاحية (super_admin, admin, user) |
| `created_at` | Timestamp | تاريخ الإنشاء |

**2. جدول المنتجات (Products)**
*(نفس السابق)*

**3. جدول الطلبات (Orders) - *تحديث***
| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | Integer/UUID | رقم الطلب |
| `user_id` | Integer | (Foreign Key) يربط بجدول المستخدمين |
| `status` | Enum | الحالة (Pending, Approved, Rejected) |
| `note` | Text | ملاحظات إضافية |
| `processed_by_id` | Integer | (Foreign Key) معرف الأدمن الذي عالج الطلب |
| `rating` | Integer | (جديد) تقييم الخدمة من 1-5 |
| `created_at` | Timestamp | وقت الإنشاء |
| `processed_at` | Timestamp | وقت المعالجة |

**4. جدول تفاصيل الطلب (OrderItems)**
*(نفس السابق)*

---

### ب) نقاط الاتصال المطلوبة (API Endpoints) - *محدث*

#### 🔐 المصادقة وإدارة المستخدمين (Auth & Users)
*   `POST /api/login`: للتحقق وإرجاع Token + Role.
*   `GET /api/users`: (للأدمن) عرض المستخدمين.
*   `POST /api/users`: (للأدمن) إنشاء مستخدم.
*   `DELETE /api/users/:id`: (للأدمن) حذف مستخدم.
*   `PUT /api/users/:id/role`: (للـ Super Admin فقط) تغيير صلاحية مستخدم.

#### 📦 المنتجات (Products)
*(نفس السابق)*

#### 📝 الطلبات (Orders)
*   `POST /api/orders`: لإنشاء طلب جديد.
*   `GET /api/orders`: للأدمن (كل الطلبات) وللمستخدم (طلباته فقط).
*   `PUT /api/orders/:id/status`: الموافقة/الرفض + تسجيل `processed_by`.
*   `PUT /api/orders/:id/rating`: للمستخدم لتقييم طلب مكتمل.

#### 📊 التقارير (Reports)
*   `GET /api/reports/ratings`: لجلب إحصائيات تقييمات الأدمنز.

---

## 4. اعتبارات هامة للمطور (Backend Developer Logic)
1.  **التحقق من الصلاحيات (Authorization):** يجب التأكد في كل Request أن المستخدم يملك الصلاحية المناسبة (مثلاً: حذف الأدمن لا يقوم به إلا Super Admin).
2.  **التحقق من المخزون (Validation):** قبل الموافقة، خصم المخزون في Transaction.
3.  **الأمان:** تشفير كلمات المرور (Hashing) وعدم تخزينها كنص واضح.

---

## 5. خطوات التسليم المتوقعة
1.  إنشاء قاعدة البيانات والجداول الجديدة.
2.  بناء الـ API مع نظام Auth متكامل (JWT مثلاً).
3.  ربط الواجهة الأمامية بالـ API.
