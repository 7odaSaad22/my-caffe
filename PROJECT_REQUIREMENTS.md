# 📋 توثيق مشروع "مقهى الشركة" ومتطلبات الباك إند (Backend)

## 1. نبذة عن المشروع
المشروع عبارة عن تطبيق ويب داخلي لشركة يهدف لتنظيم عملية طلب المشروبات من البوفيه/المقهى الخاص بالشركة.
**الحالة الحالية:** يعمل بالكامل على الواجهة الأمامية (Frontend) ويستخدم الـ LocalStorage في المتصفح لحفظ البيانات مؤقتاً.

---

## 2. المميزات الحالية (Frontend Features)
### 👤 واجهة الموظف (User)
*   **تسجيل الدخول:** عبر اختيار صفة "موظف" وكلمة مرور موحدة (`1234`).
*   **عرض القائمة:** تصفح المشروبات المتاحة ومعرفة حالة توفرها (متوفر/نفذت الكمية).
*   **سلة الطلبات:** إضافة وعرض عدة أصناف في سلة واحدة.
*   **إرسال الطلب:** إدخال اسم الموظف وملاحظات إضافية (مثل: سكر زيادة).
*   **متابعة الطلبات:** عرض سجل بطلبات الموظف السابقة وحالتها (قيد الانتظار، موافق عليه، مرفوض) بناءً على الاسم.

### 🔒 واجهة المسؤول (Admin)
*   **تسجيل الدخول:** عبر اختيار صفة "أدمن" وكلمة مرور (`admin`).
*   **إدارة الطلبات:**
    *   عرض الطلبات الجديدة "قيد الانتظار".
    *   قبول الطلب (يخصم الكمية تلقائياً من المخزون).
    *   رفض الطلب.
    *   تسجيل اسم الأدمن الذي قام بالمعالجة.
*   **إدارة المخزون:**
    *   عرض المنتجات والكميات الحالية.
    *   إضافة منتجات جديدة.
    *   حذف منتجات.
    *   تعديل كمية المخزون (إضافة رصيد).
*   **التقارير:** عرض سجل كامل بالطلبات المنفذة والمرفوضة ومن قام بها.

---

## 3. المطلوب من الباك إند (Backend Requirements)
لتحويل هذا المشروع من مجرد واجهة (Frontend) إلى تطبيق حقيقي يعمل على سيرفر وقاعدة بيانات، نحتاج لتطوير الـ API وقاعدة البيانات التالية:

### أ) قاعدة البيانات المقترحة (Database Schema)
يفضل استخدام قاعدة بيانات علاقية (مثل PostgreSQL أو MySQL) أو NoSQL (مثل MongoDB).

**1. جدول المنتجات (Products)**
| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | Integer/UUID | المعرف الفريد للمنتج |
| `name` | String | اسم المشروب (شاي، قهوة...) |
| `stock` | Integer | الكمية المتاحة حالياً |
| `is_active` | Boolean | (اختياري) لإخفاء منتج بدل حذفه |

**2. جدول الطلبات (Orders)**
| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | Integer/UUID | رقم الطلب |
| `employee_name` | String | اسم الموظف صاحب الطلب |
| `status` | Enum | الحالة (Pending, Approved, Rejected) |
| `note` | Text | ملاحظات إضافية |
| `created_at` | Timestamp | وقت إنشاء الطلب |
| `processed_at` | Timestamp | وقت المعالجة (موافقة/رفض) |
| `processed_by` | String | اسم الأدمن الذي عالج الطلب |

**3. جدول تفاصيل الطلب (OrderItems)**
*لربط الطلبات بالمنتجات (علاقة Many-to-Many)*
| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | Integer | معرف فريد |
| `order_id` | Integer | (Foreign Key) يربط بجدول الطلبات |
| `product_id` | Integer | (Foreign Key) يربط بجدول المنتجات |
| `quantity` | Integer | الكمية المطلوبة من هذا الصنف |

---

### ب) نقاط الاتصال المطلوبة (API Endpoints)

#### 🔐 المصادقة (Auth) - *اختياري في المرحلة الأولى*
*   `POST /api/login`: للتحقق من كلمة المرور وإرجاع Token (للموظف أو الأدمن).

#### 📦 المنتجات (Products)
*   `GET /api/products`: لجلب قائمة المشروبات وحالات التوفر (للموظف والأدمن).
*   `POST /api/products`: لإضافة منتج جديد (للأدمن).
*   `DELETE /api/products/:id`: لحذف منتج (للأدمن).
*   `PUT /api/products/:id/stock`: لتحديث كمية المخزون (للأدمن).

#### 📝 الطلبات (Orders)
*   `POST /api/orders`: لإنشاء طلب جديد (يستقبل اسم الموظف، الملاحظات، وقائمة الأصناف).
*   `GET /api/orders`: لجلب كل الطلبات (مع إمكانية الفلترة حسب الحالة `?status=pending`).
*   `GET /api/orders/user/:name`: لجلب تاريخ طلبات موظف معين.
*   `PUT /api/orders/:id/status`: لتغيير حالة الطلب (Approve/Reject).
    *   **هام:** عند الـ Approve، يجب أن يقوم الباك إند بخصم الكمية من جدول المنتجات والتأكد من توفرها أولاً (Transaction).

---

## 4. اعتبارات هامة للمطور (Backend Developer Logic)
1.  **التحقق من المخزون (Validation):**
    *   قبل الموافقة على الطلب، يجب التأكد أن `stock >= requested_quantity`.
    *   إذا كان المخزون غير كافٍ، يجب إرجاع خطأ للأدمن.
2.  **التزامن (Concurrency):**
    *   ماذا لو طلب موظفان آخر قطعة في نفس اللحظة؟ يجب معالجة ذلك (Database Locking أو Atomic Updates).
3.  **الأمان (Security):**
    *   حماية الـ APIs الخاصة بالأدمن (مثل الحذف والتعديل) بحيث لا يمكن لأي شخص استدعاؤها إلا بمصادقة صحيحة.

---

## 5. خطوات التسليم المتوقعة
1.  إنشاء قاعدة البيانات والجداول.
2.  بناء الـ API باستخدام لغة السيرفر (Node.js, Python, PHP, etc.).
3.  ربط الواجهة الأمامية (الموجودة حالياً) بالـ API بدلاً من `localStorage`.
